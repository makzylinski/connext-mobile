import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    flex: 1,
    justifyContent: "center",
  },
  input: {
    borderWidth: 1,
    borderColor: "#aaa",
    padding: 12,
    marginBottom: 12,
    borderRadius: 4,
  },
  error: {
    marginBottom: 12,
    color: "red",
  },
});
