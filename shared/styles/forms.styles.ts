import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
  container: {
    padding: 16,
    flex: 1,
    justifyContent: "center",
  },
  input: {
    borderWidth: 1,
    borderColor: "#fff",
    color: "#fff",
    padding: 12,
    marginBottom: 12,
    borderRadius: 4,
  },
  label: {
    color: "#fff",
  },
});
